#transcribe #whisper #python 

1. Install whisper

`pip install faster-whisper`

2. Install ffmpeg

On Linux:

``` powershell
sudo apt update
sudo apt install ffmpeg
```

On Windows:

``` PowerShell
choco install ffmpeg
```